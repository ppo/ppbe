---
interface Props {
  name: string;
  label?: string;
  size?: number;
}
const { name, label, size = 24 } = Astro.props;
---


<img
  src={`/icons/${name}.svg`} height={size} width={size} alt={`Icon: ${label || name}`}
  class="icon w-4 h-4"
/>
