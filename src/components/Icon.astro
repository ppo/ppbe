---
interface Props {
  name: string;
  label?: string;
}
const { name, label } = Astro.props;

const size = 16;
---


<img
  src={`/icons/${name}.svg`} height={size} width={size} alt={`Icon: ${label || name}`}
  class="icon w-4 h-4"
/>
