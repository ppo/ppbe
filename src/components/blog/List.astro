---
import ListItem from '@/components/blog/ListItem.astro';


interface Props {
  articles: Collection<'blog'>,
};
const { articles } = Astro.props;
---


<div class="not-prose flex flex-col space-y-10">
  {articles.map((article) => (
    <ListItem {article} />
  ))}
</div>
